{
  "name": "@fathom/jobs",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "[ -z \"$TRIGGER_SECRET_KEY\" ] && echo 'Trigger.dev: no TRIGGER_SECRET_KEY set, skipping (mock mode)' || trigger dev",
    "deploy": "trigger deploy"
  },
  "dependencies": {
    "@fathom/core": "workspace:*",
    "@fathom/db": "workspace:*",
    "@fathom/integrations": "workspace:*",
    "@trigger.dev/sdk": "^3.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@fathom/config": "workspace:*"
  }
}
